package employee;

// abstract class to prevent taking an object from it directly but by using inheritance
// public keyword to use it as a public at all the program in , out the package without restricts
// creating enum to be used as a collection of constant values that will be used at all the project
public enum Gender {
    male,
    female
}
